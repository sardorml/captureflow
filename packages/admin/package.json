{
  "name": "@captureflow/admin",
  "version": "0.1.0",
  "private": true,
  "description": "Instance-admin domain logic: operator auth, cross-tenant queries, and the audit trail. Consumed by apps/admin.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5",
    "vitest": "^4.1.9"
  }
}
