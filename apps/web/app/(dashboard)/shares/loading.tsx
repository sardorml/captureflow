import { DashboardLoading } from '../_skeletons';

// Instant placeholder while Next.js streams the real page in after the D1 query lands.
export default function Loading() {
  return <DashboardLoading variant="grid" />;
}
