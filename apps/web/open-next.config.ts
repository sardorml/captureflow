// OpenNext configuration for Cloudflare Workers + Workers Assets.
// Defaults only — no ISR or background tasks.

import { defineCloudflareConfig } from '@opennextjs/cloudflare';

export default defineCloudflareConfig({});
