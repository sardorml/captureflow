node_modules/
.nx/
dist/
out/
.next/
.open-next/
.wrangler/
.vite/
*.tsbuildinfo
next-env.d.ts

# WXT (apps/extension) — generated build output and types
.output/
.wxt/

# secrets / local env
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# logs / os
*.log
.DS_Store

# local agent tooling — skills installed via `skills add`, not part of the product
.agents/
skills-lock.json

# stale build caches (turborepo removed in favor of nx)
.turbo/

# Python bytecode (scripts/)
__pycache__/
*.pyc

# scripts/ intermediates — the captures the store and README generators compose
# from, and where the store generator writes before the tiles are copied over.
scripts/shots/
scripts/store5/
